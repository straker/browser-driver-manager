#!/usr/bin/env node

const browserDriverManager = require('../src/browser-driver-manager.js');

browserDriverManager(process.argv.slice(2)).catch(error => {
  console.error(error)
  process.exit(1)
})
